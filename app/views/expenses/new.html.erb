<section>
  <div class="bg-primary p-4 d-flex align-items-center justify-content-between">
    <%= link_to image_tag("icons8-izquierda-30.png"), category_path(@category) %>
    <h2 class="fs-4 text-white font">New transaction</h2>
  </div>

  <%= render "form", expense: @expense, category: @category %>
</section>